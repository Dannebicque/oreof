<turbo-stream action="replace" target="sidebar-tree">
    <template>
        {{ include('parcours_v2/_sideBarStructure.html.twig') }}
    </template>
</turbo-stream>

{{ include('turbo/toast.stream.html.twig', { message: toastMessage, undo: undo|default(null), type: toastType|default('success') }) }}

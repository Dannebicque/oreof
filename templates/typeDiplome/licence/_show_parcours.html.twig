<div class="col">
    {#    <section class="bg-white rounded-[2.5rem] p-10 border border-slate-100 shadow-sm"> #}
    {#        <div class="flex items-center gap-4 mb-8 border-b border-slate-50 pb-6"> #}
    {#            <div class="w-12 h-12 bg-indigo-600 rounded-2xl flex items-center justify-center text-white shadow-lg shadow-indigo-100"> #}
    {#                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="借 10h m-1 1h12m-12 5h12m-12 5h12"></path></svg> #}
    {#            </div> #}
    {#            <h3 class="text-2xl font-black text-slate-800 tracking-tight italic uppercase">Identité de la formation</h3> #}
    {#        </div> #}

    {#        <div class="grid grid-cols-1 md:grid-cols-3 gap-8"> #}
    {#            <div class="space-y-1"> #}
    {#                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest">Référence interne</p> #}
    {#                <p class="font-bold text-slate-700 text-lg">#212 <span class="text-slate-300 font-normal ml-2">| RNCP 40115</span></p> #}
    {#            </div> #}

    {#            <div class="space-y-1"> #}
    {#                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest">Niveaux (Entrée/Sortie)</p> #}
    {#                <div class="flex items-center gap-2"> #}
    {#                    <span class="px-2 py-1 bg-slate-100 rounded-lg text-xs font-bold text-slate-600">Bac</span> #}
    {#                    <svg class="w-4 h-4 text-slate-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M13 7l5 5m0 0l-5 5m5-5H6"></path></svg> #}
    {#                    <span class="px-2 py-1 bg-indigo-50 rounded-lg text-xs font-bold text-indigo-600">Bac +3</span> #}
    {#                </div> #}
    {#            </div> #}

    {#            <div class="space-y-1"> #}
    {#                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest">Responsable</p> #}
    {#                <p class="font-bold text-slate-700">Maxime GOMMEAUX</p> #}
    {#                <a href="mailto:..." class="text-xs text-indigo-500 hover:underline">maxime.gommeaux@univ-reims.fr</a> #}
    {#            </div> #}

    {#            <div class="md:col-span-3 p-6 bg-slate-50 rounded-3xl border border-slate-100 grid md:grid-cols-2 gap-6"> #}
    {#                <div> #}
    {#                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">Diplôme & Spécialité</p> #}
    {#                    <p class="font-bold text-slate-800 uppercase italic">Licence Sciences de la vie et de la Terre (SVT)</p> #}
    {#                </div> #}
    {#                <div> #}
    {#                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">Composante porteuse</p> #}
    {#                    <p class="font-bold text-slate-600 italic">UFR Sciences Exactes et Naturelles</p> #}
    {#                </div> #}
    {#            </div> #}
    {#        </div> #}
    {#    </section> #}

    {#    <section class="max-w-4xl space-y-8"> #}
    {#        <div class="prose prose-slate prose-sm max-w-none"> #}
    {#            <h3 class="text-indigo-600 font-black uppercase tracking-tighter italic text-3xl mb-8">Organisation & Objectifs</h3> #}

    {#            <div class="not-prose bg-white border-l-4 border-indigo-500 p-8 rounded-r-[2.5rem] shadow-sm mb-10"> #}
    {#                <p class="text-slate-600 leading-relaxed font-medium"> #}
    {#                    Le parcours <span class="text-indigo-600 font-black">BGG</span> est une formation généraliste pluridisciplinaire. #}
    {#                    Il aborde les différents aspects des sciences de la vie (biologie, écologie, physiologie) et de la terre. #}
    {#                </p> #}
    {#            </div> #}

    {#            <div class="grid md:grid-cols-2 gap-12 not-prose"> #}

    {#                <div class="space-y-4"> #}
    {#                    <h4 class="flex items-center gap-2 text-slate-800 font-black uppercase text-xs tracking-widest"> #}
    {#                        <span class="w-2 h-2 bg-indigo-500 rounded-full"></span> #}
    {#                        Objectifs du parcours #}
    {#                    </h4> #}
    {#                    <p class="text-slate-500 text-sm leading-relaxed"> #}
    {#                        Acquérir des connaissances fondamentales et des compétences dans les domaines des sciences de la vie et de la terre, ainsi qu'une culture naturaliste. #}
    {#                    </p> #}
    {#                </div> #}
    {#                <div class="space-y-4"> #}
    {#                    <h4 class="flex items-center gap-2 text-slate-800 font-black uppercase text-xs tracking-widest"> #}
    {#                        <span class="w-2 h-2 bg-emerald-500 rounded-full"></span> #}
    {#                        Rythme & Format #}
    {#                    </h4> #}
    {#                    <div class="bg-slate-100 p-4 rounded-2xl"> #}
    {#                        <p class="font-bold text-slate-700">Temps plein</p> #}
    {#                        <p class="text-[10px] text-slate-400 italic">Présentiel obligatoire</p> #}
    {#                    </div> #}
    {#                </div> #}
    {#            </div> #}
    {#        </div> #}
    {#    </section> #}

    <section
            data-scroll-spy-target="section"
            class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
            id="identite_formation">
        {{ include('communs/_header_show.html.twig', {
            title: 'Identité de la formation',
            icon: 'fa-solid fa-id-card',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_identiteFormation.html.twig') }}
        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="presentation">
        {{ include('communs/_header_show.html.twig', {
            title: 'Présentation formation ' ~ parcours.formation.display,
            icon: 'fa-solid fa-book-open',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_presentationFormation.html.twig') }}

        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="presentation_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Présentation du parcours ' ~ parcours.libelle,
            icon: 'fa-solid fa-book-open',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_presentationParcours.html.twig', {parcours:parcours}) }}
        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="descriptif_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Descriptif du parcours ' ~ parcours.libelle,
            icon: 'fa-solid fa-book-open',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_descriptifParcours.html.twig', {parcours:parcours}) }}
        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="localisation_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Localisation du parcours ' ~ parcours.libelle,
            icon: 'fa-solid fa-map-marker-alt',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_localisationParcours.html.twig', {parcours:parcours}) }}

        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="competences_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Compétences Acquises du parcours ' ~ parcours.libelle,
            icon: 'fa-solid fa-tasks',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_competences.html.twig', {parcours:parcours}) }}
        </div>
    </section>

    {% if isVersioning is not defined or isVersioning == false %}
        <section data-scroll-spy-target="section"
                 class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
                 id="structure_{{ parcours.id }}">
            {{ include('communs/_header_show.html.twig', {
                title: 'Structure du parcours ' ~ parcours.libelle,
                icon: 'fa-solid fa-sitemap',
                color: 'orange'
            }) }}

            <div>
                {{ render(controller('App\\Controller\\StructureShowController::parcoursShow', {
                    parcours: parcours.id,
                    hasLastVersion: hasLastVersion ?? false,
                })) }}
            </div>
        </section>
    {% else %}
        {# Visionnage d'une version #}
        <section data-scroll-spy-target="section"
                 class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
                 id="structure_{{ parcours.id }}">
            {{ include('communs/_header_show.html.twig', {
                title: 'Structure du parcours ' ~ parcours.libelle,
                icon: 'fa-solid fa-sitemap',
                color: 'orange'
            }) }}
            <div>
                {{ include('typeDiplome/' ~ constant('TEMPLATE_FOLDER', typeD) ~ '/structure/_structure.html.twig',
                    {
                        parcours: parcours,
                        dto: dto,
                        hasLastVersion: false,
                        diffStructure: null
                    }
                ) }}
            </div>
        </section>
    {% endif %}
    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="admission_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Admission du parcours ' ~ parcours.libelle,
            icon: 'fa-solid fa-user-check',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_admission.html.twig', {parcours:parcours}) }}
        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="inscription_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Inscription du parcours ' ~ parcours.libelle,
            icon: 'fa-solid fa-pen-to-square',
            color: 'indigo'
        }) }}

        <div>
            {{ include('typeDiplome/formation/_inscription.html.twig', {parcours:parcours}) }}
        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="et_apres_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Et après...',
            icon: 'fa-solid fa-briefcase',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_etApres.html.twig', {parcours:parcours}) }}
        </div>
    </section>

    <section data-scroll-spy-target="section"
             class="bg-white rounded-sm p-4 mb-4 border border-slate-200 shadow-sm scroll-mt-32"
             id="contacts_{{ parcours.id }}">
        {{ include('communs/_header_show.html.twig', {
            title: 'Contacts pédagogiques',
            icon: 'fa-solid fa-address-book',
            color: 'indigo'
        }) }}
        <div>
            {{ include('typeDiplome/formation/_contacts.html.twig', {parcours:parcours}) }}
        </div>
    </section>
</div>

{% macro displayMccc(currentValue, previousValue, suffixString = "", withComparaison = false) %}
    {% if withComparaison %}
        {% if currentValue != previousValue %}
            {% if previousValue != false %}
            <span class="text-danger text-decoration-line-through mx-2">
                {{ previousValue }}{{ suffixString }}
            </span>
            {% endif %}
            {% if currentValue != false %}
            <span class="text-success mx-2">
                {{ currentValue }}{{ suffixString }}
            </span>
            {% endif %}
        {% else %}
            {{ currentValue }}{{ suffixString }}
        {% endif %}
    {% else %}
        {{ currentValue }}{{ suffixString }}
    {% endif %}
{% endmacro %}
<div class="col">
    <section class="scroll-section" id="identite_formation">
        <h2 class="small-title">Identité de la formation</h2>
        <div class="card mb-5">
            <div class="card-body">
                {{ include('typeDiplome/formation/_identiteFormation.html.twig') }}
            </div>
        </div>
    </section>

    <section class="scroll-section" id="localisation">
        <h2 class="small-title">Localisation</h2>
        <div class="card mb-5">
            <div class="card-body">
                {{ include('typeDiplome/formation/_localisation.html.twig') }}
            </div>
        </div>
    </section>

    <section class="scroll-section" id="presentation">
        <h2 class="small-title">Présentation</h2>
        <div class="card mb-5">
            <div class="card-body">
                {{ include('typeDiplome/formation/_presentationFormation.html.twig') }}

            </div>
        </div>
    </section>
    {% if formation.isHasParcours == false and formation.parcours|length == 1 %}
        <section class="scroll-section" id="descriptif">
            <h2 class="small-title">Descriptif</h2>
            <div class="card mb-5">
                <div class="card-body">
                    {{ include('typeDiplome/formation/_descriptif.html.twig', {parcours:formation.parcours[0]}) }}
                </div>
            </div>
        </section>

        <section class="scroll-section" id="competences">
            <h2 class="small-title">Compétences acquises</h2>
            <div class="card mb-5">
                <div class="card-body">
                    {{ include('typeDiplome/formation/_competences.html.twig', {parcours:formation.parcours[0]}) }}
                </div>
            </div>
        </section>

        <section class="scroll-section" id="structure">
            <h2 class="small-title">Structure</h2>
            <div class="card mb-5">
                <div class="card-body">
                    {{ include('typeDiplome/formation/_structure.html.twig', {parcours:formation.parcours[0]}) }}
                </div>
            </div>
        </section>

        <section class="scroll-section" id="admission">
            <h2 class="small-title">Admission</h2>
            <div class="card mb-5">
                <div class="card-body">
                    {{ include('typeDiplome/formation/_admission.html.twig', {parcours:formation.parcours[0]}) }}

                </div>
            </div>
        </section>

        {#        <section class="scroll-section" id="inscription">#}
        {#            <h2 class="small-title">Inscription</h2>#}
        {#            <div class="card mb-5">#}
        {#                <div class="card-body">#}
        {#                    {{ include('typeDiplome/_inscription.html.twig', {parcours:formation.parcours[0]}) }}#}

        {#                </div>#}
        {#            </div>#}
        {#        </section>#}

        <section class="scroll-section" id="et_apres">
            <h2 class="small-title">Et après ?</h2>
            <div class="card mb-5">
                <div class="card-body">
                    {{ include('typeDiplome/formation/_etApres.html.twig', {parcours:formation.parcours[0]}) }}

                </div>
            </div>
        </section>

        <section class="scroll-section" id="contacts">
            <h2 class="small-title">Contacts pédagogiques</h2>
            <div class="card mb-5">
                <div class="card-body">
                    {{ include('typeDiplome/formation/_contacts.html.twig', {parcours:formation.parcours[0]}) }}

                </div>
            </div>
        </section>

    {% else %}
        {% for parcours in formation.parcours %}
            <div>
                <h3>Parcours {{ parcours.libelle }}</h3>
            </div>
            <section class="scroll-section" id="presentation_{{ parcours.id }}">
                <h2 class="small-title">Présentation</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_presentationParcours.html.twig', {parcours:parcours}) }}

                    </div>
                </div>
            </section>

            <section class="scroll-section" id="descriptif_{{ parcours.id }}">
                <h2 class="small-title">Descriptif de la formation</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_descriptif.html.twig', {parcours:parcours}) }}

                    </div>
                </div>
            </section>

            <section class="scroll-section" id="localisation_{{ parcours.id }}">
                <h2 class="small-title">Localisation</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_localisationParcours.html.twig', {parcours:parcours}) }}

                    </div>
                </div>
            </section>

            <section class="scroll-section" id="competences_{{ parcours.id }}">
                <h2 class="small-title">Compétences Acquises</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_competences.html.twig', {parcours:parcours}) }}

                    </div>
                </div>
            </section>

            <section class="scroll-section" id="structure_{{ parcours.id }}">
                <h2 class="small-title">Structure</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_structure.html.twig', {parcours:parcours}) }}
                    </div>
                </div>
            </section>

            <section class="scroll-section" id="admission_{{ parcours.id }}">
                <h2 class="small-title">Admission</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_admission.html.twig', {parcours:parcours}) }}
                    </div>
                </div>
            </section>

            <section class="scroll-section" id="inscription_{{ parcours.id }}">
                <h2 class="small-title">Inscription</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_inscription.html.twig', {parcours:parcours}) }}
                    </div>
                </div>
            </section>

            <section class="scroll-section" id="et_apres_{{ parcours.id }}">
                <h2 class="small-title"> Et après...</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_etApres.html.twig', {parcours:parcours}) }}
                    </div>
                </div>
            </section>

            <section class="scroll-section" id="contacts_{{ parcours.id }}">
                <h2 class="small-title">Contacts pédagogiques</h2>
                <div class="card mb-5">
                    <div class="card-body">
                        {{ include('typeDiplome/formation/_contacts.html.twig', {parcours:parcours}) }}
                    </div>
                </div>
            </section>
        {% endfor %}
    {% endif %}
</div>

<!-- Nav Content Start -->
{#    <div id="nav" class="nav-container d-flex" #}
    {#         data-controller="nav" #}
    {#         data-action="click@window->nav#_onWindowClick keydown.escape@window->nav#_hideMobileMenu"> #}
    {#        <div class="nav-content d-flex"> #}
    {#            <!-- Logo Start --> #}
    {#            <div class="logo-univ"> #}
    {#                <img src="{{ asset('images/logo_urca_quadri.png') }}" #}
    {#                     alt="Logo de l'URCA" #}
    {#                     width="75px"/> #}
    {#            </div> #}
    {#            <div class="logo position-relative"> #}
    {#                <a href="{{ path('app_homepage') }}"> #}
    {#                    <div class="logo-but"> #}
    {#                        <i class="fa-duotone fa-pencil-ruler" style="--fa-primary-color: var(--complementaire); #}
    {# "></i>&nbsp;{{ product_name }} #}
    {#                        {% if is_granted('ROLE_ADMIN') %} / Adm.{% endif %} #}
    {#                    </div> #}
    {#                </a> #}
    {#            </div> #}
    {#            <!-- Logo End --> #}

    {#            <!-- Notifications --> #}
    {#            {{ component('notifications', { #}
    {#                user: app.user, #}
    {#                formation: formation is defined ? formation : null, #}
    {#                parcours: parcours is defined ? parcours : null, #}
    {#            }) }} #}


<nav class="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
    <div class="max-w-full mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">

            <div class="flex items-center space-x-8">
                <div class="flex-shrink-0 flex items-center font-bold text-indigo-600 text-xl tracking-tight">
                    ORé<span class="text-gray-900">OF</span>
                    {% if is_granted('ROLE_ADMIN') %}<span class="text-red-900"> / Adm.</span>{% endif %}
                </div>

                <div class="hidden md:flex items-center space-x-4">
                    <a href="{{ path('app_homepage') }}"
                       class="text-gray-600 hover:text-indigo-600 px-3 py-2 text-sm font-medium transition">{{ 'menu.tableau_de_bord'|trans({}, 'menu') }}</a>

                    {# Menu offre de formation, accessible à tous #}
                    <div class="relative" data-controller="navbar-dropdown"
                         data-action="click@window->navbar-dropdown#close">
                        <button data-action="click->navbar-dropdown#toggle"
                                class="flex items-center text-gray-600 hover:text-indigo-600 px-3 py-2 text-sm font-medium">
                            {{ 'menu.offre_formation'|trans({}, 'menu') }}
                            <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </button>
                        <div data-navbar-dropdown-target="content"
                             class="hidden absolute left-0 mt-2 w-48 bg-white border border-gray-200 rounded-lg shadow-xl py-2">
                            <a href="{{ path('app_formation_index') }}"
                               class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.detail_mentions'|trans({}, 'menu') }}</a>
                            <a href="{{ path('structure_fiche_matiere_index') }}"
                               class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.detail_fiches'|trans({}, 'menu') }}</a>
                            <a href="{{ path('structure_fiche_matiere_index', {type:'hd'}) }}"
                               class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.detail_fiches_hd'|trans({}, 'menu') }}</a>
                            <a href="{{ path('app_export_index') }}"
                               class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.exports'|trans({}, 'menu') }}</a>
                        </div>
                    </div>
                    {# Fin Menu offre de formation #}

                    {% if is_granted('ROLE_ADMIN') %}
                        {% set menu_admin_pilotage = [
                            {
                                'path' : path('structure_composante_index'),
                                'label' : 'menu.admin.consulter_offre'|trans({}, 'menu')},
                            {
                                'path' : path('app_offre'),
                                'label' : 'menu.admin.gestion_offre'|trans({}, 'menu')},
                            {
                                'path' : path('app_demande_dpe'),
                                'label' : 'menu.admin.dpe_ouverts'|trans({}, 'menu')},
                            {
                                'path' : path('app_validation_dpe_index'),
                                'label' : 'menu.admin.validation_dpe'|trans({}, 'menu')},
                            {
                                'path' : path('app_validation_fiche_index'),
                                'label' : 'menu.admin.validation_fiches_ec'|trans({}, 'menu')},
                            {
                                'path' : path('app_validation_change_rf_index'),
                                'label' : 'menu.admin.validation_rf'|trans({}, 'menu')},
                            {
                                'path' : path('app_parcours_lheo_invalid_list'),
                                'label' : 'menu.admin.controle_lheo'|trans({}, 'menu'),
                            },
                            {
                                'path' : path('app_export_index'),
                                'label' : 'menu.admin.exports'|trans({}, 'menu'),
                            },{
                                'path' : path('app_log_viewer_index'),
                                'label' : 'menu.admin.logs.applicatifs'|trans({}, 'menu'),
                            },{
                                'path' : path('app_impersonation_list'),
                                'label' : 'menu.admin.impersonation.user'|trans({}, 'menu'),
                            }] %}
                        <div class="relative" data-controller="navbar-dropdown"
                             data-action="click@window->navbar-dropdown#close">
                            <button data-action="click->navbar-dropdown#toggle"
                                    class="flex items-center text-gray-600 hover:text-indigo-600 px-3 py-2 text-sm font-medium">
                                {{ 'menu.admin.pilotage'|trans({}, 'menu') }}
                                <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </button>
                            <div data-navbar-dropdown-target="content"
                                 class="hidden absolute left-0 mt-2 w-48 bg-white border border-gray-200 rounded-lg shadow-xl py-2">
                                {% for item in menu_admin_pilotage %}
                                    <a href="{{ item.path }}"
                                       class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ item.label }}</a>
                                {% endfor %}
                            </div>
                        </div>
                    {% else %}
                        {% if is_granted('SHOW', {route: 'app_composante', subject: 'composante'}) %}

                            <div class="static" data-controller="navbar-dropdown"
                                 data-action="click@window->navbar-dropdown#close">
                                <button data-action="click->navbar-dropdown#toggle"
                                        class="flex items-center text-gray-600 hover:text-indigo-600 px-3 py-2 text-sm font-medium">
                                    {{ 'menu.compo.pilotage'|trans({}, 'menu') }}
                                    <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path d="M19 9l-7 7-7-7"></path>
                                    </svg>
                                </button>
                                <div data-navbar-dropdown-target="content"
                                     class="hidden absolute inset-x-0 top-16 bg-white border-b border-gray-200 shadow-2xl overflow-hidden">
                                    <div class="max-w-7xl mx-auto grid grid-cols-3 gap-8 p-8">
                                        {% for composante in app.user.composanteResponsableDpe %}
                                            {% if is_granted('SHOW', {route: 'app_composante', subject: composante}) %}
                                                <div>
                                                    <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-4">{{ composante.libelle }}</h3>
                                                    <ul class="space-y-3">
                                                        <li><a href="{{ path('structure_composante_index') }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.consulter_offre'|trans({}, 'menu') }}</a>
                                                        </li>
                                                        <li>
                                                            <a href="{{ path('app_demande_dpe_composante', {composante:composante.id}) }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.dpe_ouvert'|trans({}, 'menu') }}</a>
                                                        </li>
                                                        <li>
                                                            <a href="{{ path('app_validation_composante_dpe_index', {composante:composante.id}) }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.validation_dpe'|trans({}, 'menu') }}</a>
                                                        </li>
                                                        <li>
                                                            <a href="{{ path('app_validation_composante_fiche_index', {composante:composante.id}) }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.validation_ec'|trans({}, 'menu') }}</a>
                                                        </li>
                                                        <li>
                                                            <a href="{{ path('app_validation_composante_change_rf_index', {composante:composante.id}) }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.validation_rf'|trans({}, 'menu') }}</a>
                                                        </li>
                                                        <li>
                                                            <a href="{{ path('app_validation_composante_pilotage', {composante:composante.id}) }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.pilotage_visuel'|trans({}, 'menu') }}</a>
                                                        </li>
                                                        <li>
                                                            <a href="{{ path('app_export_composante_index', {composante:composante.id}) }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.exports'|trans({}, 'menu') }}</a>
                                                        </li>
                                                        <li>
                                                            <a href="{{ path('app_plaquette', {composante:composante.id}) }}"
                                                               class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.compo.communication'|trans({}, 'menu') }}</a>
                                                        </li>
                                                    </ul>
                                                </div>
                                            {% endif %}
                                        {% endfor %}

                                        <div class="bg-gray-50 p-6 rounded-xl">
                                            <p class="font-bold text-gray-900 mb-2 italic text-sm text-indigo-600">
                                                Aide &
                                                Support</p>
                                            <p class="text-xs text-gray-600 leading-relaxed">Accédez à tous les
                                                guides
                                                administratifs et aux contacts de la scolarité centrale.</p>
                                        </div>
                                    </div>
                                </div>
                            </div>


                        {% endif %}
                    {% endif %}

                    {% if is_granted('EDIT', {route: 'app_etablissement', subject: 'etablissement'}) or is_granted('ROLE_ADMIN') %}
                        <div class="relative" data-controller="navbar-dropdown"
                             data-action="click@window->navbar-dropdown#close">
                            <button data-action="click->navbar-dropdown#toggle"
                                    class="flex items-center text-gray-600 hover:text-indigo-600 px-3 py-2 text-sm font-medium">
                                {{ 'menu.menu_conseils'|trans({}, 'menu') }}
                                <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </button>
                            <div data-navbar-dropdown-target="content"
                                 class="hidden absolute left-0 mt-2 w-48 bg-white border border-gray-200 rounded-lg shadow-xl py-2">
                                <a href="{{ path('app_formation_responsable_liste') }}"
                                   class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.conseils.chgt_rf'|trans({}, 'menu') }}</a>
                                <a href="{{ path('app_synthese_modification_export_pdf') }}"
                                   class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.conseils.synthses_modifs'|trans({}, 'menu') }}</a>
                            </div>
                        </div>
                    {% endif %}

                    {% if is_granted('EDIT', {route: 'app_composante', subject: 'composante'}) or is_granted('ROLE_ADMIN') %}
                        <div class="relative" data-controller="navbar-dropdown"
                             data-action="click@window->navbar-dropdown#close">
                            <button data-action="click->navbar-dropdown#toggle"
                                    class="flex items-center text-gray-600 hover:text-indigo-600 px-3 py-2 text-sm font-medium">
                                {{ 'menu.menu_droits'|trans({}, 'menu') }}
                                <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </button>
                            <div data-navbar-dropdown-target="content"
                                 class="hidden absolute left-0 mt-2 w-48 bg-white border border-gray-200 rounded-lg shadow-xl py-2">
                                <a href="{{ path('app_user_profil_attente') }}"
                                   class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.droits.en_attente'|trans({}, 'menu') }}</a>
                                <a href="{{ path('app_user_profil_index') }}"
                                   class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.droits.affectation_profils'|trans({}, 'menu') }}</a>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <a href="{{ path('app_user_repertoire') }}"
                                       class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.droits.repertoire'|trans({}, 'menu') }}</a>
                                    <a href="{{ path('app_administration_profils_index') }}"
                                       class="block px-4 py-2 text-sm hover:bg-indigo-50">{{ 'menu.droits.profils'|trans({}, 'menu') }}</a>
                                {% endif %}
                            </div>
                        </div>
                    {% endif %}

                    {% if is_granted('ROLE_ADMIN') %}
                        <div class="static" data-controller="navbar-dropdown"
                             data-action="click@window->navbar-dropdown#close">
                            <button data-action="click->navbar-dropdown#toggle"
                                    class="flex items-center text-gray-600 hover:text-indigo-600 px-3 py-2 text-sm font-medium">
                                {{ 'menu.menu_administration'|trans({}, 'menu') }}
                                <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </button>
                            <div data-navbar-dropdown-target="content"
                                 class="hidden absolute inset-x-0 top-16 bg-white border-b border-gray-200 shadow-2xl overflow-hidden">
                                <div class="max-w-7xl mx-auto grid grid-cols-4 gap-8 p-8">
                                    <div>
                                        <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-4">
                                            {{ 'menu.config.menu_etablissement'|trans({}, 'menu') }}
                                        </h3>
                                        <ul class="space-y-3">
                                            <li><a href="{{ path('app_etablissement_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.etablissement'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_composante_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.composante'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_ville_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.ville'|trans({}, 'menu') }}</a>
                                            </li>
                                        </ul>
                                    </div>
                                    <div>
                                        <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-4">
                                            {{ 'menu.config.offre_formation'|trans({}, 'menu') }}
                                        </h3>
                                        <ul class="space-y-3">
                                            <li><a href="{{ path('app_domaine_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.domaines'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_mention_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.mentions'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li>
                                                <a href="{{ path('app_type_diplome_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.type_diplome'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_type_ue_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.type_ue'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_type_ec_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.type_ec'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_nature_ue_ec_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.nature_ue'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_type_epreuve_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.type_epreuve'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_langue_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.langues'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_rythme_formation_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.rythme_formation'|trans({}, 'menu') }}</a>
                                            </li>
                                        </ul>
                                    </div>
                                    <div>
                                        <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-4">
                                            {{ 'menu.menu_configuration'|trans({}, 'menu') }}
                                        </h3>
                                        <ul class="space-y-3">
                                            <li><a href="{{ path('app_campagne_collecte_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.campagne_collecte'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_annee_universitaire_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.annee_universitaire'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_actualite_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.actualites'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_notication_liste') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.notifications'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('translations_index') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.traductions'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li><a href="{{ path('app_config_email') }}"
                                                   class="text-sm text-gray-700 hover:text-indigo-600">{{ 'menu.config.config_emails'|trans({}, 'menu') }}</a>
                                            </li>
                                            <li>
                                                <a href="{{ path('app_help_index') }}">
                                                    <span class="label">{{ 'menu.config.help'|trans({}, 'menu') }}</span>
                                                </a>
                                            </li>
                                            <li>
                                                <a href="{{ path('app_faq_index') }}">
                                                    <span class="label">{{ 'menu.config.faq'|trans({}, 'menu') }}</span>
                                                </a>
                                            </li>
                                        </ul>
                                    </div>
                                    <div class="bg-gray-50 p-6 rounded-xl">
                                        <p class="font-bold text-gray-900 mb-2 italic text-sm text-indigo-600">Aide
                                            &
                                            Support</p>
                                        <p class="text-xs text-gray-600 leading-relaxed">Accédez à tous les guides
                                            administratifs et aux contacts de la scolarité centrale.</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>

            {# Menu aligné droite #}
            <div class="flex items-center space-x-6">

                {# Remplacé barre de recherche legacy par le composant d'icônes Tailwind #}
                {{ component('notifications', {
                    user: app.user,
                    formation: formation is defined ? formation : null,
                    parcours: parcours is defined ? parcours : null
                }) }}

                {# Menu Campagne collecte #}
                <div class="relative" data-controller="navbar-dropdown"
                     data-action="click@window->navbar-dropdown#close">
                    <button data-action="click->navbar-dropdown#toggle"
                            class="flex items-center bg-indigo-50 text-indigo-700 px-3 py-1.5 rounded-lg text-xs font-bold border border-indigo-100 hover:bg-indigo-100 transition">
                        <span class="mr-1">{{ user_data.campagneCollecte != null ? user_data.campagneCollecte.libelle|trim|raw : 'erreur' }}</span>
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path d="M19 9l-7 7-7-7"></path>
                        </svg>
                    </button>
                    <div data-navbar-dropdown-target="content"
                         class="hidden absolute right-0 mt-2 w-32 bg-white border border-gray-200 rounded-lg shadow-lg z-50">
                        {% for dpe in user_data.dpes %}
                            <a href="{{ path('app_switch_dpe', {id:dpe.id}) }}"
                               class="block px-4 py-2 text-xs {% if dpe.id == user_data.campagneCollecte.id %}text-indigo-600 bg-indigo-50 font-bold{% else %}hover:bg-gray-50{% endif %}">{{ dpe.libelle }}</a>
                        {% endfor %}
                    </div>
                </div>
                {# Fin Menu Campagne collecte #}

                {# Menu User #}
                <div class="relative" data-controller="navbar-dropdown"
                     data-action="click@window->navbar-dropdown#close">
                    <button data-action="click->navbar-dropdown#toggle"
                            class="flex items-center focus:outline-none">
                        <div class="h-9 w-9 rounded-full bg-indigo-600 flex items-center justify-center text-white text-sm font-bold ring-2 ring-offset-2 ring-transparent hover:ring-indigo-500 transition-all">
                            {# todo: couleur selon les droits ? #}
                            {{ app.user.avatarInitiales }}
                        </div>
                    </button>
                    <div data-navbar-dropdown-target="content"
                         class="hidden absolute right-0 mt-2 w-56 bg-white border border-gray-200 rounded-xl shadow-2xl py-2 overflow-hidden">
                        <div class="px-4 py-3 border-b border-gray-100 bg-gray-50/50">
                            <p class="text-sm font-bold text-gray-900">{{ app.user.display }}</p>
                            <p class="text-xs text-gray-500 truncate">{{ app.user.email }}</p>
                        </div>
                        <a href="{{ path('app_user_mes_informations') }}"
                           class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-indigo-50">
                            <svg class="w-4 h-4 mr-3 text-gray-400" fill="none" stroke="currentColor"
                                 viewBox="0 0 24 24">
                                <path d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                            </svg>
                            Mes informations
                        </a>
                        <a href="{{ path('app_user_mes_notifications') }}"
                           class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-indigo-50">
                            <svg class="w-4 h-4 mr-3 text-gray-400" fill="none" stroke="currentColor"
                                 viewBox="0 0 24 24">
                                <path d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                            </svg>
                            Mes notifications
                        </a>
                        {#                                    <button type="button" #}
                        {#                                            class="w-100 text-start bg-transparent border-0 p-0" #}
                        {#                                            data-action="click->theme#toggle" #}
                        {#                                            title="Basculer entre mode clair et mode sombre" #}
                        {#                                    > #}
                        {#                                        <i class="fas fa-circle-half-stroke"></i> #}
                        {#                                        <span data-theme-target="label">Basculer le thème</span> #}
                        {#                                    </button> #}
                        {#                                {% if is_granted('ROLE_ADMIN') %} #}
                        {#                                    <li> #}
                        {#                                        <a href="{{ path('i18n_toggle_keys') }}"> #}
                        {#                                            {{ app.session.get('i18n_keys') ? '🔤 Mode clés: ON' : '🔤 Mode clés: OFF' }} #}
                        {#                                        </a> #}
                        {#                                    </li> #}
                        {#                                {% endif %} #}
                        <a href="{{ path('app_accessibilite') }}"
                           class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-indigo-50"
                           data-turbo="true"
                           data-action="click->modalturbo#open">
                            <svg class="w-4 h-4 mr-3 text-gray-400" fill="none" stroke="currentColor"
                                 viewBox="0 0 24 24">
                                <path d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                                <circle cx="12" cy="12" r="3"></circle>
                            </svg>
                            Parametres & accessibilite
                        </a>
                        {#                                    <a #}
                        {#                                            href="#" #}
                        {#                                            {{ stimulus_controller('modal', { #}
                        {#                                                size: 'lg', #}
                        {#                                                modalTitle: 'Signaler un bug', #}
                        {#                                                modalUrl: path('app_bug_report_modal', { #}
                        {#                                                    formation: formation is defined and formation != null ? formation.id : '', #}
                        {#                                                    parcours: parcours is defined and parcours != null ? parcours.id : '', #}
                        {#                                                    page: app.request.uri #}
                        {#                                                }) #}
                        {#                                            }) }} #}
                        {#                                            data-action="click->modal#openModal" #}
                        {#                                    > #}
                        {#                                        Signaler un bug #}
                        {#                                    </a> #}
                        <hr class="my-1 border-gray-100">
                        <a href="{{ path('app_logout') }}"
                           class="flex items-center px-4 py-2 text-sm text-red-600 hover:bg-red-50">
                            <svg class="w-4 h-4 mr-3 text-red-500" fill="none" stroke="currentColor"
                                 viewBox="0 0 24 24">
                                <path d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                            </svg>
                            Déconnexion
                        </a>
                        {#                                    <a #}
                        {#                                            href="#" #}
                        {#                                            {{ stimulus_controller('modal', { #}
                        {#                                                size: 'lg', #}
                        {#                                                modalTitle: 'Signaler un bug', #}
                        {#                                                modalUrl: path('app_bug_report_modal', { #}
                        {#                                                    formation: formation is defined and formation != null ? formation.id : '', #}
                        {#                                                    parcours: parcours is defined and parcours != null ? parcours.id : '', #}
                        {#                                                    page: app.request.uri #}
                        {#                                                }) #}
                        {#                                            }) }} #}
                        {#                                            data-action="click->modal#openModal" #}
                        {#                                    > #}
                        {#                                        Signaler un bug #}
                        {#                                    </a> #}
                    </div>
                </div>
                {# Fin Menu User #}
            </div>
            {# Fin Menu aligné droite #}
        </div>
    </div>
</nav>
<!-- Nav Content End -->

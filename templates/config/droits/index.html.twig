{% extends 'base.html.twig' %}

{% block content %}
    {{ component('alerte', {type:"info", message:'Le ROLE_ADMIN dispose de l\'ensemble des accès.'}) }}

    <div class="card mt-2"
            {{ stimulus_controller('crud', {
                url: path('app_droits_access_liste')
            }) }}
         data-action="base:refreshListe@window->crud#refreshListe">
        <div class="card-body" {{ stimulus_target('crud', 'liste') }}>
        </div>
    </div>
{% endblock %}

{% block header %}
    Gestion des droits d'accès et des rôles
{% endblock %}

{% block breadcrumb %}
    {{ include('communs/_breadcrumb.html.twig', {liens:[
        {
            href:'',
            title: 'Administration',
        }
    ]}) }}
{% endblock %}

{% block bouton_header %}
    <button
            {{ stimulus_controller('modal', {
                size: 'lg',
                modalTitle: 'Ajouter un rôle',
                modalUrl: path('app_droits_access_new') }) }}
            data-action="click->modal#openModal"
            class="btn btn-sm btn-outline-success btn-icon btn-icon-end w-100 w-sm-auto mx-2"
            data-size="lg"
            data-bs-toggle="tooltip"
            data-bs-placement="bottom"
            title="Ajouter un rôle"
    >
        <i class="{{ iconAdd }}"></i>
        <span> Ajouter un rôle</span>
    </button>
{% endblock %}

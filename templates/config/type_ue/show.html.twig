<table class="table">
    <tbody>
    <tr>
        <th>Libelle</th>
        <td>{{ type_ue.libelle }}</td>
    </tr>
    <tr>
        <th>Type de diplôme</th>
        <td>
            {% for typeDip in type_ue.typeDiplomes %}
                {{ typeDip|typeDiplome }}
                {% if loop.last == false %}
                    ,
                {% endif %}
            {% endfor %}
        </td>
    </tr>
    </tbody>
</table>

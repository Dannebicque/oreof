{% extends 'base.html.twig' %}

{% block content %}
    <div class="card mt-2"
            {{ stimulus_controller('crud', {
                url: path('app_actualite_liste')
            }) }}
         data-action="base:refreshListe@window->crud#refreshListe">
        <div class="card-body" {{ stimulus_target('crud', 'liste') }}>
        </div>
    </div>
{% endblock %}

{% block header %}
    Gestion des actualités
{% endblock %}

{% block breadcrumb %}
    {{ include('communs/_breadcrumb.html.twig', {liens:[
        {
            href:'',
            title: 'Administration',
        }
    ]}) }}
{% endblock %}

{% block bouton_header %}
    <button
            {{ stimulus_controller('modal', {
                size: 'lg',
                modalTitle: 'Ajouter une actualité',
                modalUrl: path('app_actualite_new') }) }}
            data-action="click->modal#openModal"
            class="btn btn-sm btn-outline-success btn-icon btn-icon-end w-100 w-sm-auto mx-2"
            data-size="lg"
            data-bs-toggle="tooltip"
            data-bs-placement="bottom"
            title="Ajouter une actualité"
    >
        <i class="{{ iconAdd }}"></i>
        <span> Ajouter une actualité</span>
    </button>
{% endblock %}

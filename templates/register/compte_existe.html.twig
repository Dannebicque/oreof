{% extends 'base-speciale.html.twig' %}

{% block content %}
    <div class="mb-2 mt-4">
        <h2 class="cta-1 mb-0 text-primary">Bienvenue,</h2>
        <h2 class="cta-1 text-primary">Un compte existe déjà</h2>
    </div>

    {% if user != null %}
    <div>
        <p><strong>{{ user.display }},</strong></p>
        <p>Votre demande n'a pu aboutir, un compte semble déjà présent pour cet email : {{ user.email }}.</p>
        <p>Si vous n'êtes pas à l'origine de la demande, merci de contacter l'administrateur : {{ mail_contact|mailto }}.</p>
    </div>
    {% else %}
        <div>
            <p>Votre demande n'a pu aboutir, car aucun compte ne correspond cet email : {{ email }}.</p>
            <p>Merci de contacter l'administrateur : {{ mail_contact|mailto }}.</p>
        </div>
    {% endif %}
    <p class="text-center mt-3">OU</p>
    <div class="d-grid">
        <a href="{{ path('app_login') }}" class="btn d-block btn-primary">Connectez vous</a>
    </div>
{% endblock %}

{% block title %}
Compte déjà existant
{% endblock %}


{{ component('alerte', {
    type: 'info',
    message: ('deposer.fichier.valide.alerte')|trans({}, 'process')|nl2br|raw
}) }}

{% form_theme form 'communs/form_theme_v2.html.twig' %}

{{ form_start(form, {
    attr: {
        id: 'modal_form'
    }
}) }}
{{ form_end(form) }}

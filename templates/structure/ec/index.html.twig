{% extends 'base.html.twig' %}

{% block content %}
    <div class="card"
            {{ stimulus_controller('crud', {
                url: path('structure_ec_liste'),
            }) }}
         data-action="base:refreshListe@window->crud#refreshListe base:modalClose@window->crud#refreshListe"
    >
        <div class="card-body">
            {{ include('communs/_recherche.html.twig') }}
            <div {{ stimulus_target('crud', 'liste') }}>

            </div>
        </div>
    </div>
{% endblock %}

{% block header %}
    Gestion des éléments constitutifs
{% endblock %}

{% block breadcrumb %}
    {{ include('communs/_breadcrumb.html.twig', {liens:[
    ]}) }}
{% endblock %}

{% block bouton_header %}
    {% if is_granted('ROLE_FORMATION', app.user) and not is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_ec_etat_send_ouverture') }}"
           class="btn btn-sm btn-outline-warning btn-icon btn-icon-end w-100 w-sm-auto mx-2"
           data-bs-toggle="tooltip"
           data-bs-placement="bottom"
           title=" Ouvrir les EC à la rédaction"
        >
            <i class="{{ iconAdd }}"></i>
            <span> Ouvrir les EC à la rédaction</span>
        </a>
    {% endif %}
{% endblock %}

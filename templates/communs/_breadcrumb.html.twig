<nav aria-label="Fil d'Ariane" class="pt-0">
    <ol class="flex flex-wrap items-center gap-1.5 text-sm">
        <li>
            {% if liens is empty %}
                {# Accueil = page courante #}
                <span aria-current="page"
                      class="inline-flex items-center gap-1 rounded-md bg-slate-100 px-2 py-1 font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">
                    <twig:UX:Icon name="icon:home" class="h-4 w-4" aria-hidden="true"/>
                    <span>Accueil</span>
                </span>
            {% else %}
                <a href="{{ path('app_homepage') }}"
                   class="inline-flex items-center gap-1 rounded-md px-2 py-1 text-slate-500 transition hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-200">
                    <twig:UX:Icon name="icon:home" class="h-4 w-4" aria-hidden="true"/>
                    <span>Accueil</span>
                </a>
            {% endif %}
        </li>

        {% for lien in liens %}
            <li class="inline-flex items-center gap-1.5">
                <twig:UX:Icon name="icon:chevron-right" class="h-3.5 w-3.5 text-slate-500 dark:text-slate-400"
                              aria-hidden="true"/>

                {% if loop.last %}
                    {# Page courante : pas de lien, aria-current #}
                    <span aria-current="page"
                          class="rounded-md bg-slate-100 px-2 py-1 font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200">
                        {{ lien.title }}
                    </span>
                {% elseif lien.href is defined and lien.href is not empty %}
                    {# Lien intermédiaire navigable #}
                    <a href="{{ lien.href }}"
                       class="rounded-md px-2 py-1 text-slate-500 transition hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-200">
                        {{ lien.title }}
                    </a>
                {% else %}
                    {# Étape intermédiaire sans lien (section non cliquable) #}
                    <span class="rounded-md px-2 py-1 text-slate-500 dark:text-slate-400">
                        {{ lien.title }}
                    </span>
                {% endif %}
            </li>
        {% endfor %}
    </ol>
</nav>

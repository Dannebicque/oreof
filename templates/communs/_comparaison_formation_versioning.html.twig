{% if is_granted('ROLE_ADMIN') %}
    {% if stringData is defined 
        and stringData != null
        and stringData|length > 0
    %}
        <span class="badge rounded-pill text-bg-primary mt-2 mb-1">
            Différences
        </span>
        <div class="row mt-1 mb-4">
            <div class="col-6">
                {{ stringData|raw }}  
            </div> 
        </div>
    {% endif %}
{% endif %}
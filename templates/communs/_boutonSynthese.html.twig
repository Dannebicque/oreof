{% set buttonSet = synthese_buttons(parcours, false) %}
{{ include('communs/_boutonSynthese_renderer.html.twig', { buttonSet: buttonSet, col: col, iconDownload: iconDownload|default('fas fa-download') }) }}

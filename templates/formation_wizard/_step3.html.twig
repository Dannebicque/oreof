<h2>Compétences acquises</h2>
<div {{ stimulus_controller('formation--step3', {
    url: path('app_formation_save', {formation:formation.id}),
    urlListe: path('app_bloc_competence_liste', {formation:formation.id}),
}) }}
        data-action="base:refreshListe@window->formation--step3#refreshListe"
>
    <div {{ stimulus_target('formation--step3', 'liste') }}>

    </div>
    <button
            {{ stimulus_controller('modal', {
                size: 'lg',
                modalTitle: 'Ajouter un bloc de compétences',
                modalUrl: path('app_bloc_competence_new', {formation:formation.id}) }) }}
            data-action="click->modal#openModal"
            class="btn btn-sm btn-outline-success btn-icon btn-icon-end w-100 w-sm-auto mx-2 mt-4"
            data-size="lg"
            data-bs-toggle="tooltip"
            data-bs-placement="bottom"
            title="Ajouter un bloc de compétences"
    >
        <i class="{{ iconAdd }}"></i>
        <span> Ajouter un bloc de compétences</span>
    </button>
</div>

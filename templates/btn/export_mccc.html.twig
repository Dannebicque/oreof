{% if hasLastVersion is defined and hasLastVersion == true %}
    {{ component('dropdown_actions', {
        label: 'MCCC',
        help: 'Exports MCCC',
        icon: 'icon:download',
        labelSrOnly: false,
        items: [
            {
                label: 'Export MCCC Version (xlsx)',
                icon: 'teenyicons:spreadsheet-outline',
                url: path('app_parcours_mccc_export_versionning', {parcours: parcours.id, _format: 'xlsx'})
            },
            {
                label: 'Version complete (pdf)',
                icon: 'teenyicons:pdf-outline',
                url: path('app_parcours_mccc_export_cfvu_valid', {parcours: parcours.id, format: 'complet'})
            },
            {
                label: 'Version simplifiee (pdf)',
                icon: 'teenyicons:pdf-outline',
                url: path('app_parcours_mccc_export_cfvu_valid', {parcours: parcours.id, format: 'simplifie'})
            }
        ]
    }) }}
{% else %}
    {{ component('dropdown_actions', {
        label: 'MCCC',
        help: 'Exports MCCC',
        icon: 'icon:download',
        labelSrOnly: false,
        items: [
            {
                label: 'Version complete (xlsx)',
                icon: 'teenyicons:spreadsheet-outline',
                url: path('app_parcours_mccc_export', {parcours: parcours.id, _format: 'xlsx'})
            },
            {
                label: 'Version simplifiee (xlsx)',
                icon: 'teenyicons:spreadsheet-outline',
                url: path('app_parcours_mccc_export_light', {parcours: parcours.id, _format: 'xlsx'})
            },
            {
                label: 'Version complete (pdf)',
                icon: 'teenyicons:pdf-outline',
                url: path('app_parcours_mccc_export_cfvu_valid', {parcours: parcours.id, format: 'complet'})
            },
            {
                label: 'Version simplifiee (pdf)',
                icon: 'teenyicons:pdf-outline',
                url: path('app_parcours_mccc_export_cfvu_valid', {parcours: parcours.id, format: 'simplifie'})
            }
        ]
    }) }}
{% endif %}

{% extends 'mails/base_mail.html.twig' %}

{% block content %}
    <p>Bonjour {{ responsableDpe.display }},</p>

    {% if hasParcours == true %}
        <p>Le parcours <strong>{{ parcours.libelle }}</strong> de la formation <strong>{{ formation.displayLong }}</strong>, vient d'être validé et soumis par son responsable.</p>
    {% else %}
        <p>La formation <strong>{{ formation.displayLong }}</strong> vient d'être validée et soumise par son responsable.</p>
    {% endif %}


    <p>Vous pouvez maintenant le valider, le refuser ou émettre des réserves en cliquant sur le lien suivant :</p>

    {% if hasParcours == true %}
        <p><a href="{{ url('app_parcours_edit', {'id': parcours.id}) }}">{{ url('app_parcours_edit', {'id': parcours.id}) }}</a></p>
    {% else %}
        <p><a href="{{ url('app_formation_edit', {'slug': formation.slug}) }}">{{ url('app_formation_edit', {'slug': formation.slug}) }}</a></p>
    {% endif %}
{% endblock %}
